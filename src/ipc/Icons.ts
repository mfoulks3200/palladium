/**
 * Icon type for IPC layer. Uses plain strings to avoid React/Lucide
 * dependencies in shared code. The renderer resolves these to actual
 * components at render time.
 */
export type IpcIcons = string;
