import { FileTree } from '@/components/ui/file-tree';
import {
  ResizableHandle,
  ResizablePanel,
  ResizablePanelGroup,
} from '@/components/ui/resizable';

export const EditorPage = () => {
  return <>Testing</>;
};
